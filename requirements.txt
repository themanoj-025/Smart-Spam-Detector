# ============================================================
# Streamlit Cloud — Deployment Dependencies
# ============================================================

# Core ML & Data
numpy>=2.2.6,<2.3.0
scikit-learn>=1.3.0
pandas>=2.0.0,<3.0

# Web UI
streamlit>=1.28.0

# REST API (api.py is a production entrypoint in Docker/CI — these were
# previously only in pyproject.toml/requirements-dev.txt and are required
# at runtime: fastapi, uvicorn, slowapi rate limiting, python-multipart
# for UploadFile/Form parsing)
fastapi>=0.100.0
uvicorn>=0.23.0
slowapi>=0.1.9
python-multipart>=0.0.6

# NLP & Parsing (email body extraction from HTML)
beautifulsoup4>=4.12.0

# Explainability (SHAP word-level explanations)
# NOTE: shap>=0.45.0 ships manylinux wheels so no C compilation needed on
# Streamlit Cloud. We intentionally omit an upper bound so pip can resolve
# a version compatible with the installed numpy/scipy.
shap>=0.45.0

# Visualization (radar charts, confusion matrices in Model Comparison tab)
plotly>=5.15.0

# FastAPI test compatibility (pin httpx to avoid starlette/httpx API conflict)
httpx<0.29.0

# Excel upload support (pd.read_excel in Batch Processing tab)
openpyxl>=3.1.0

# MLflow experiment tracking & model registry (optional)
mlflow>=3.15.1

# API observability
prometheus-client>=0.26.0

# OpenTelemetry distributed tracing (optional, enabled via OTEL_ENABLED=true)
opentelemetry-api>=1.20.0,<2.0
opentelemetry-sdk>=1.20.0,<2.0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0,<2.0
opentelemetry-instrumentation-fastapi>=0.41b0,<1.0
