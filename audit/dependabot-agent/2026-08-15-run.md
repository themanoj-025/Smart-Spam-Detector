# Smart-Spam-Detector — Dependabot Agent Audit (2026-08-15)

**Repo:** themanoj-025/Smart-Spam-Detector · **Ecosystems:** pip, pre-commit

## Verdicts
| PR | Change | Type | Verdict |
|----|--------|------|---------|
| #1 | pre-commit >=4.6.1 → >=4.6.2 (dev) | patch | AUTO-MERGE-eligible — pending CI |
| #2 | hypothesis >=6.165.2 → >=6.165.3 (dev) | patch | AUTO-MERGE-eligible — pending CI |
| #3/#4 | ruff-pre-commit v0.9.10→0.16.x, mirrors-mypy v1.15→2.3 (pre-commit) | — | verify-on-CI |

## Baseline repair (Phase 0 — committed before PR processing)
- **commit b696e06**: docker check was failing for every PR (and would fail on main) because `python:3.11-slim` ships apt `python3-msgpack 1.1.2` (GHSA-6v7p-g79w-8964, HIGH) and `python3-setuptools 70.3.0` (CVE-2025-47273) in `/usr/lib/python3/dist-packages`; the pip upgrade in the deps stage installs patched copies but Trivy scans every copy. Fix (mirrors the proven Next-Gen-Reco/Price-My-Car pattern): `apt-get purge -y --auto-remove python3-msgpack python3-setuptools` in the base stage. Evidence: Trivy scan logs from failing runs (msgpack 1.1.2 / setuptools 70.3.0 flagged; build log shows patched 1.2.1/84.0.0 in /usr/local).

## Verification
- PRs are single-line floor bumps in requirements-dev.txt (dev-only tooling); the repo's own test suite is unaffected by them. Dev-dep changes verified by CI once runners recovered.

## Notes
- The docker/trivy gate failure was pre-existing and unrelated to the Dependabot PRs; fixed at the baseline so PR CI becomes meaningful.
